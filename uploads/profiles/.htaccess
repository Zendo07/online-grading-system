# Allow only image files
<FilesMatch "\.(jpg|jpeg|png|gif)$">
    Require all granted
</FilesMatch>

# Deny access to PHP files
<FilesMatch "\.php$">
    Require all denied
</FilesMatch>

# Disable directory browsing
Options -Indexes